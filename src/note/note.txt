interceptors added when all request need to add common header or something like that scenario

if we introduce multiple interceptors the order is important of app.module
because that order follows when execute interceptors
